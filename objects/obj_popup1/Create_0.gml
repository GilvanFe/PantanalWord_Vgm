/// Create Event do objeto popup

// Define a posição inicial do popup fora da tela, abaixo da parte inferior
popup_y = 2100;
// Define a velocidade de movimento do popup (quanto menor o valor, mais devagar o popup vai subir)
popup_speed = 10; 
// Define se o popup está visível
popup_visible = true;

audio_play_sound(WIN_SOUND,10,false);